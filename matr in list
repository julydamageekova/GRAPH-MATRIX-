   
   
   
   matrixinlist(vector < vector < int > &G, int V, bool**G1){
        for(int i = 0; i < V; i++){
            for(int j = 0; j < V; j++){
                    if(G[i][j]==1)
                        G[i].push_back(j);
            }
        }
   }
  
